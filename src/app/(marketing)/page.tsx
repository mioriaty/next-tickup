import React from 'react';

import LoginButton from '@/app/(marketing)/_components/LoginButton';

interface PageProps {}

const Page: React.FC<PageProps> = () => {
  return (
    <div>
      <LoginButton />
    </div>
  );
};

export default Page;
